import { createTheme } from '@mui/material';

export const lightMode = createTheme({
    palette:{
        mode: 'light',
        primary: {
            main: '#5603AD'
        },
        secondary:{
            main: '#8367C7'
        }
    }
})